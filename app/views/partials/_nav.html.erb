<header class="navbar navbar-expand-lg navbar navbar-dark bg-dark fixed-top nav-shadow">
    <a class="navbar-brand" href="/">Instagramm</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-md-auto d-md-flex">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_pic_path %>">Create Pic</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= edit_user_registration_path %>">Edit Profile</a>
          </li>
          <li class="nav-item">
            <a class='nav-link' data-method='delete' href="<%= destroy_user_session_path %>">Logout</a>
          </li>
          <% else %>
          <li class="nav-item">
            <a class="nav-link"  href="<%= new_user_session_path %>">Login</a>
          </li>
        <li class="nav-item">
          <a class="btn btn-primary d-lg-inline-block mb-3 mb-md-0 ml-md-3"  href="<%= new_user_registration_path %>">Create Account</a>
        </li>
          <%end%>
      </ul>
    </div>
</header>
